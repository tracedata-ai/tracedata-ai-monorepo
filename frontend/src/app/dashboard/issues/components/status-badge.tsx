export interface StatusBadgeProps {
  status: string; // "Resolved" | "Open" | "Overdue" | "Closed"
}

export function StatusBadge({ status }: StatusBadgeProps) {
  // YOUR TURN: Move the logic from renderStatusBadge here
  // Return the appropriate styled <span> based on the status prop.
  
  return null; // Replace me
}
