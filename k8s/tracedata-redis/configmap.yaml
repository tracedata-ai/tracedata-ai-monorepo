apiVersion: v1
kind: ConfigMap
metadata:
  name: tracedata-redis-config
  namespace: tracedata
  labels:
    app: tracedata-redis
    tier: cache
    environment: prod
data:
  REDIS_MAXMEMORY: "256mb"
  REDIS_MAXMEMORY_POLICY: "allkeys-lru"
