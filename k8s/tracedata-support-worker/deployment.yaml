apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracedata-support-worker
  namespace: tracedata
  labels:
    app: tracedata-support-worker
    tier: worker
    environment: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tracedata-support-worker
  template:
    metadata:
      labels:
        app: tracedata-support-worker
        tier: worker
        environment: prod
    spec:
      containers:
        - name: tracedata-support-worker
          image: sreerajrone/tracedata-backend-agent-api:sha-ae8a037  # backend-tag
          imagePullPolicy: Always
          command:
            - celery
            - "-A"
            - celery_app
            - worker
            - "-Q"
            - "td:agent:support"
            - "-n"
            - "support@%h"
            - "--loglevel=info"
          envFrom:
            - configMapRef:
                name: tracedata-support-worker-config
            - secretRef:
                name: tracedata-api-secret
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
