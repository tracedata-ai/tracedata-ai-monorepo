# common/db

This directory contains the database engine, migrations, and repositories.

- **engine.py**: Async SQLAlchemy engine and session factory.
- **migrations/**: SQL scripts for database schema evolution.
- **repositories/**: Data access layer (DAL) for specific entities (events, trips, scores, etc.).
