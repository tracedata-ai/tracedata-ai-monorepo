# common/redis

This directory contains Redis client logic and key schema.

- **client.py**: `RedisClient` for prioritized push/pop operations and Pub/Sub signalling.
- **keys.py**: `RedisSchema` defining all key patterns and TTL constants.
