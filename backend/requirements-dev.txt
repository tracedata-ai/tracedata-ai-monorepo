# Dev-only — NOT installed in the Docker runtime image
# Install: pip install -r requirements-dev.txt

pytest==8.2.0
pytest-asyncio==0.23.6   # async test support for FastAPI route testing
httpx==0.27.0            # async test client (TestClient replacement for async)
black==24.4.2            # code formatter — run before every commit
ruff==0.4.4              # linter (replaces flake8 + isort)
mypy==1.10.0             # static type checker
